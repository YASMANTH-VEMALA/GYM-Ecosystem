{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && npm ci --include=dev",
  "buildCommand": "node -e \"require('fs').rmSync('/vercel/.cache/prisma', { recursive: true, force: true })\" && cd ../.. && npm run db:generate --workspace=@gymstack/db",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
